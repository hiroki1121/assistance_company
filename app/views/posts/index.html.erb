<%# 背景画像 %>
<div class="back-img">
<%= render "shared/header" %>
<%# 画像上文字列 %>
  <div class="app-description">
    <p class="description-text">Assistance Companyとは...</p>
    <p class="description-text">・外注したいけど、どこに頼んでいいか分からない</p>
    <p class="description-text">・自社の情報をもっと発信したい</p>
    <p class="description-text">そんな方にオススメのサービスです！</p>
    <p class="app-title-script">New encounter</p>
  </div>
</div>
<body>
  <%# 検索バー %>
  <div class="search-bar">
    <i class="fas fa-search">会社を検索する</i>
    <%= search_form_for @q, url: search_posts_path, class: "search-form" do |f| %>
    <%= f.collection_select :industry_id_eq, Industry.where.not(id: 1), :id, :name, include_blank: '業種を選択', class:"search-select-box" %>
    <i class="fas fa-times"></i>
    <%= f.collection_select :prefecture_id_eq, Prefecture.where.not(id: 1), :id, :name, include_blank: '都道府県を選択', class:"search-select-box" %>
    <i class="fas fa-times"></i>
    <%= f.search_field :company_name_or_division_or_representative_name_or_business_detail_cont, placeholder: "キーワードを記入", class: "input-box" %>
    <%= f.submit "検索", class: "search-btn" %>
    <% end %>
  </div>
<%# 会社一覧 %>
  <div class="title-bar-main">
    <h1 class="title-bar-main-content">会社一覧</h1>
  </div>
  <div class="company-lists">
    <% @posts.each do |post| %>
      <%= render "shared/company_digest", post: post %>
    <% end %>
  </div>
</body>
<%= render "shared/footer" %>
